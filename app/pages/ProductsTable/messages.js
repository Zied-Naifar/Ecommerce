/*
 * ProductsTable Messages
 *
 * This contains all the text for the ProductsTable container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.pages.ProductsTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductsTable container!',
  },
});
